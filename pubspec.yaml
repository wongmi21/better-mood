name: better_mood

version: 0.0.1

environment:
  sdk: '>=2.1.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  firebase_core:
  firebase_analytics:
  firebase_auth:

  google_sign_in:
  flutter_facebook_login:

flutter:
  uses-material-design: true
  assets:
    - assets/
