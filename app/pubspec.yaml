name: better_mood

version: 0.0.1

environment:
  sdk: '>=2.1.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_crashlytics:

  fish_redux:
  
  firebase_core:
  firebase_analytics:
  firebase_auth:

  flutter_facebook_login:
  google_sign_in:

flutter:
  uses-material-design: true
  assets:
    - assets/
